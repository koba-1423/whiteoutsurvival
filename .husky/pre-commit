#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 行数制限チェック（300行を超えるファイルがあるとコミットをブロック）
echo "🔍 ファイル行数チェック中..."
npm run check-lines

if [ $? -ne 0 ]; then
  echo "❌ エラー: 300行を超えるファイルがあります！"
  echo "📝 ファイルを分割してからコミットしてください。"
  exit 1
fi

echo "✅ 行数チェック完了！"