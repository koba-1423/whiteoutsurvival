import * as THREE from "three";
import { CollisionBox } from "../rendering/SceneManager.js";

/**
 * 仲間エリアを作成
 * 仲間を雇ったり管理する場所
 */
export function createCompanionArea(
  _scene: THREE.Scene,
  _x: number,
  _z: number
): CollisionBox[] {
  // TODO: 仲間エリアの3Dオブジェクトを実装

  // 衝突判定用の境界ボックスを返す
  return [];
}
