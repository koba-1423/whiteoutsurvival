import * as THREE from "three";
import { CollisionBox } from "../rendering/SceneManager.js";

/**
 * 武器アップグレードエリア（鍛冶場）を作成
 * 武器をアップグレードする場所
 */
export function createForgeArea(
  _scene: THREE.Scene,
  _x: number,
  _z: number
): CollisionBox[] {
  // TODO: 鍛冶場の3Dオブジェクトを実装

  // 衝突判定用の境界ボックスを返す
  return [];
}
