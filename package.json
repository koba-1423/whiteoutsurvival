{
  "name": "whiteoutsurvival",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "check-lines": "eslint src/**/*.ts --rule 'max-lines: error'",
    "prepare": "husky"
  },
  "dependencies": {
    "three": "^0.161.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@types/three": "^0.180.0",
    "@typescript-eslint/eslint-plugin": "^8.44.1",
    "@typescript-eslint/parser": "^8.44.1",
    "eslint": "^9.36.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.1",
    "typescript": "^5.4.4",
    "vite": "^5.2.0"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "eslint --rule 'max-lines: error' --rule 'max-lines-per-function: error'"
    ]
  }
}
